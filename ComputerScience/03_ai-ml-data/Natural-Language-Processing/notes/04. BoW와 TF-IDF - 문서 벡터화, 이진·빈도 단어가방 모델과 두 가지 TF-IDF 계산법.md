---
title: 04. BoW와 TF-IDF - 문서 벡터화, 이진·빈도 단어가방 모델과 두 가지 TF-IDF 계산법
description: "문서를 수치 벡터로 바꾸는 단어가방 모델(BoW)의 두 형태(binary: 등장 여부, count: 등장 횟수)와 단어사전이 차원을 결정하는 원리, 흔한 단어의 가중치를 깎는 TF-IDF의 두 계산법(방법1 - 문서 길이 반영 TF와 IDF = ln(N/df), 방법2 - 싸이킷런 TfidfVectorizer의 IDF = ln((N+1)/(df+1))+1 및 L2 정규화), 그리고 TF는 문서마다 다른 반면 IDF는 단어당 값이 하나라는 핵심 구분을 정리한다."
type: lecture
tags:
  - natural-language-processing
  - bag-of-words
  - binary-bow
  - count-bow
  - tf-idf
  - vectorization
  - vocabulary
  - tfidf-vectorizer
course: natural-language-processing
semester: 2-2
source: nlp2026_4장_tfidf.pdf
source_pages: 18
source_type: lecture-slides
status: stable
review_state: active
authority: primary
slides: false
created: 2026-09-10
updated: 2026-09-11
---
> [!NOTE]
> 문서를 숫자 벡터로 바꾸는 두 방법 — **BoW**(등장 여부/횟수)와 **TF-IDF**(여기에 흔한 단어 감점을 더한 것).
> 출처: `nlp2026_4장_tfidf.pdf` · 전 18쪽

## 1. 문서 벡터화

텍스트를 자동 분류하려면 먼저 문서를 벡터로 바꿔야 한다 (p.3).

```mermaid
graph LR
  A["원문 텍스트"] --> B["BoW"] --> C["TF-IDF"] --> D["Word2Vec / BERT"]
```

## 2. BoW (단어가방 모델)

- 문서를 "단어들의 집합(가방)"으로 보고 **순서는 무시**한다 (p.4)
- **binary BoW** — 등장 여부 0/1 · **count BoW** — 등장 횟수
- 절차: ① 단어사전 생성(정렬해 고정) → ② 사전 순서대로 벡터 변환
- 장점: 단순하고 구현이 쉬움 / 단점: 순서·문맥·의미 소실, 차원의 저주, 희소 행렬 (p.5)

> [!IMPORTANT]
> 단어사전의 $i$번째 단어가 벡터의 $i$번째 차원이다. 그래서 **사전은 순서가 중요**하고, 단어 종류 수가 곧 벡터 차원이 된다.

### 강의 예제 (p.4)

```html preview
<div style="font-family:system-ui,sans-serif;padding:18px;color:var(--foreground)">
  <textarea id="bowDocs" rows="2" style="width:100%;margin-bottom:10px;padding:10px;font-size:14px;
    font-family:ui-monospace,SFMono-Regular,Menlo,monospace;background:var(--card);
    color:var(--card-foreground);border:1px solid var(--border);
    border-radius:var(--radius);box-sizing:border-box">나는 오늘 학교에 갔다
오늘 나는 도서관에 갔다</textarea>

  <div id="bowMode" style="display:flex;gap:8px;margin-bottom:14px"></div>
  <div id="bowVocab" style="font-size:13px;color:var(--muted-foreground);margin-bottom:12px"></div>
  <div id="bowOut" style="display:flex;flex-direction:column;gap:10px"></div>

  <script>
    var MODES = [['binary', 'Binary BoW'], ['count', 'Count BoW']];
    var mode = 'binary';
    var ta = document.getElementById('bowDocs');

    function btnStyle(on) {
      return 'padding:6px 14px;font-size:13px;font-weight:600;cursor:pointer;' +
        'border-radius:999px;border:1px solid ' + (on ? 'var(--primary)' : 'var(--border)') + ';' +
        'background:' + (on ? 'var(--primary)' : 'transparent') + ';' +
        'color:' + (on ? 'var(--primary-foreground)' : 'var(--muted-foreground)') + ';';
    }

    function drawModes() {
      document.getElementById('bowMode').innerHTML = MODES.map(function (m) {
        return '<button data-m="' + m[0] + '" style="' + btnStyle(mode === m[0]) + '">' + m[1] + '</button>';
      }).join('');
      Array.prototype.forEach.call(document.querySelectorAll('#bowMode button'), function (b) {
        b.onclick = function () { mode = b.getAttribute('data-m'); render(); };
      });
    }

    function tokenize(line) {
      return line.toLowerCase().split(/[^a-z0-9가-힣]+/).filter(function (t) { return t.length > 0; });
    }

    function render() {
      drawModes();
      var lines = ta.value.split('\n').filter(function (l) { return l.trim().length > 0; });
      var docs = lines.map(tokenize);
      var seen = {};
      docs.forEach(function (d) { d.forEach(function (t) { seen[t] = true; }); });
      var vocab = Object.keys(seen).sort();

      document.getElementById('bowVocab').innerHTML =
        '단어사전 <b>' + vocab.length + '차원</b> — <code>[' +
        vocab.map(function (w) { return "'" + w + "'"; }).join(', ') + ']</code>';

      document.getElementById('bowOut').innerHTML = docs.map(function (d, i) {
        var vec = vocab.map(function (w) {
          var c = d.filter(function (t) { return t === w; }).length;
          return mode === 'binary' ? (c > 0 ? 1 : 0) : c;
        });
        var chips = vocab.map(function (w, k) {
          var v = vec[k];
          var col = v === 0 ? 'var(--muted-foreground)' : (v > 1 ? 'var(--chart-5)' : 'var(--chart-1)');
          return '<div style="min-width:60px;text-align:center;padding:5px 7px;border-radius:var(--radius);' +
            'background:' + (v === 0 ? 'transparent' : 'var(--card)') + ';' +
            'border:1px solid ' + (v === 0 ? 'var(--border)' : col) + ';opacity:' + (v === 0 ? '0.45' : '1') + '">' +
            '<div style="font-size:11px;color:var(--muted-foreground)">' + w + '</div>' +
            '<div style="font-size:17px;font-weight:700;color:' + col + '">' + v + '</div></div>';
        }).join('');
        return '<div style="padding:12px;border:1px solid var(--border);border-radius:var(--radius)">' +
          '<div style="font-size:13px;margin-bottom:8px"><b>문서' + (i + 1) + '</b> ' +
          '<span style="color:var(--muted-foreground)">' + lines[i].trim() + '</span></div>' +
          '<div style="display:flex;gap:6px;flex-wrap:wrap;margin-bottom:8px">' + chips + '</div>' +
          '<code style="font-size:13px">[' + vec.join(', ') + ']</code></div>';
      }).join('');
    }

    ta.addEventListener('input', render);
    render();
  </script>
</div>
```

## 3. 과제 — 세 문서 벡터화

```html preview
<div style="font-family:system-ui,sans-serif;padding:18px;color:var(--foreground)">
  <textarea id="bowDocs" rows="3" style="width:100%;margin-bottom:10px;padding:10px;font-size:14px;
    font-family:ui-monospace,SFMono-Regular,Menlo,monospace;background:var(--card);
    color:var(--card-foreground);border:1px solid var(--border);
    border-radius:var(--radius);box-sizing:border-box">i love machine learning
i love deep deep learning learning
i like deep learning</textarea>

  <div id="bowMode" style="display:flex;gap:8px;margin-bottom:14px"></div>
  <div id="bowVocab" style="font-size:13px;color:var(--muted-foreground);margin-bottom:12px"></div>
  <div id="bowOut" style="display:flex;flex-direction:column;gap:10px"></div>

  <script>
    var MODES = [['binary', 'Binary BoW'], ['count', 'Count BoW']];
    var mode = 'binary';
    var ta = document.getElementById('bowDocs');

    function btnStyle(on) {
      return 'padding:6px 14px;font-size:13px;font-weight:600;cursor:pointer;' +
        'border-radius:999px;border:1px solid ' + (on ? 'var(--primary)' : 'var(--border)') + ';' +
        'background:' + (on ? 'var(--primary)' : 'transparent') + ';' +
        'color:' + (on ? 'var(--primary-foreground)' : 'var(--muted-foreground)') + ';';
    }

    function drawModes() {
      document.getElementById('bowMode').innerHTML = MODES.map(function (m) {
        return '<button data-m="' + m[0] + '" style="' + btnStyle(mode === m[0]) + '">' + m[1] + '</button>';
      }).join('');
      Array.prototype.forEach.call(document.querySelectorAll('#bowMode button'), function (b) {
        b.onclick = function () { mode = b.getAttribute('data-m'); render(); };
      });
    }

    function tokenize(line) {
      return line.toLowerCase().split(/[^a-z0-9가-힣]+/).filter(function (t) { return t.length > 0; });
    }

    function render() {
      drawModes();
      var lines = ta.value.split('\n').filter(function (l) { return l.trim().length > 0; });
      var docs = lines.map(tokenize);
      var seen = {};
      docs.forEach(function (d) { d.forEach(function (t) { seen[t] = true; }); });
      var vocab = Object.keys(seen).sort();

      document.getElementById('bowVocab').innerHTML =
        '단어사전 <b>' + vocab.length + '차원</b> — <code>[' +
        vocab.map(function (w) { return "'" + w + "'"; }).join(', ') + ']</code>';

      document.getElementById('bowOut').innerHTML = docs.map(function (d, i) {
        var vec = vocab.map(function (w) {
          var c = d.filter(function (t) { return t === w; }).length;
          return mode === 'binary' ? (c > 0 ? 1 : 0) : c;
        });
        var chips = vocab.map(function (w, k) {
          var v = vec[k];
          var col = v === 0 ? 'var(--muted-foreground)' : (v > 1 ? 'var(--chart-5)' : 'var(--chart-1)');
          return '<div style="min-width:60px;text-align:center;padding:5px 7px;border-radius:var(--radius);' +
            'background:' + (v === 0 ? 'transparent' : 'var(--card)') + ';' +
            'border:1px solid ' + (v === 0 ? 'var(--border)' : col) + ';opacity:' + (v === 0 ? '0.45' : '1') + '">' +
            '<div style="font-size:11px;color:var(--muted-foreground)">' + w + '</div>' +
            '<div style="font-size:17px;font-weight:700;color:' + col + '">' + v + '</div></div>';
        }).join('');
        return '<div style="padding:12px;border:1px solid var(--border);border-radius:var(--radius)">' +
          '<div style="font-size:13px;margin-bottom:8px"><b>문서' + (i + 1) + '</b> ' +
          '<span style="color:var(--muted-foreground)">' + lines[i].trim() + '</span></div>' +
          '<div style="display:flex;gap:6px;flex-wrap:wrap;margin-bottom:8px">' + chips + '</div>' +
          '<code style="font-size:13px">[' + vec.join(', ') + ']</code></div>';
      }).join('');
    }

    ta.addEventListener('input', render);
    render();
  </script>
</div>
```

## 4. TF-IDF

- **TF** — 이 문서에서 자주 나오는가 (자주 → 중요도 ↑)
- **IDF** — 다른 문서에도 흔한가 (흔하면 중요도 ↓)
- 비유: 모든 학생이 하는 말은 눈에 안 띄고, **특정 학생만 반복한 말**이 그 학생의 키워드다 (p.6)

> [!IMPORTANT]
> **⭐ 시험 포인트**
>
> - TF는 **(단어, 문서)의 함수** → 같은 단어라도 **문서마다 값이 다르다** ⭕
> - IDF는 **(단어)만의 함수** → 문서별로 상이하지 않고 ==단어 1개당 IDF 값은 1개==
>
> 예제(문서 4·단어 13) 기준 TF 값은 최대 52개, IDF 값은 13개. **IDF 표에는 문서 축이 없다.**

### 계산 예제 (p.8)

$N = 4$, 조사 제외 후 문서1 `나 학교 간다`(3단어) · 문서2 `학교 공부한다`(2) · 문서3 `친구 학교 만나서 다른 친구 집 갔다`(7) · 문서4 `운동 좋아하는 친구 함께 운동 했다`(6). 정렬한 사전은 13개 → **13차원**.

### 방법1 — 정규화 없음 (p.9\~12)

$$
\text{TF} = \frac{\text{빈도수}}{\text{문서 총 단어수}}
\qquad
\text{IDF} = \ln\!\left(\frac{N}{df}\right)
$$

- IDF: `학교`(df 3) 0.29 · `친구`(df 2) 0.69 · 나머지(df 1) 1.39
- 문서1 → `나` $0.33 \times 1.39 \approx 0.46$, `학교` $0.33 \times 0.29 \approx 0.10$, `간다` 0.46

#### 과제 — 방법1로 세 문서 벡터화

> [!NOTE]
> 문서1 `i love machine learning` · 문서2 `i love deep deep learning learning` · 문서3 `i like deep learning`
>
> 소수점 **4자리**까지 (5번째 자리에서 반올림). $N = 3$, 사전은 BoW 과제와 동일한 **6차원** — `0: deep  1: i  2: learning  3: like  4: love  5: machine`

**(1) TF만 반영한 벡터** (p.10) — 빈도수 ÷ 문서 총 단어수

```text
문서1 (4단어)  [0,      0.2500, 0.2500, 0,      0.2500, 0.2500]
문서2 (6단어)  [0.3333, 0.1667, 0.3333, 0,      0.1667, 0     ]
문서3 (4단어)  [0.2500, 0.2500, 0.2500, 0.2500, 0,      0     ]
```

**(2) 각 단어의 IDF** (p.11)

```text
deep      df 2  ->  ln(3/2) = 0.4055
i         df 3  ->  ln(3/3) = 0.0000
learning  df 3  ->  ln(3/3) = 0.0000
like      df 1  ->  ln(3/1) = 1.0986
love      df 2  ->  ln(3/2) = 0.4055
machine   df 1  ->  ln(3/1) = 1.0986
```

**(3) 최종 TF-IDF 벡터** — TF × IDF

```text
문서1  [0,      0, 0, 0,      0.1014, 0.2747]
문서2  [0.1352, 0, 0, 0,      0.0676, 0     ]
문서3  [0.1014, 0, 0, 0.2747, 0,      0     ]
```

> [!IMPORTANT]
> `i` 와 `learning` 은 **세 문서 전부에 등장**해 $df = N = 3$, 따라서 $\ln(3/3) = 0$ 이다. TF가 0.25든 0.3333이든 곱하면 **전부 0으로 소거**된다.
>
> 빈칸이 아니라 **IDF가 지운 것**이다 — 모든 문서에 나오는 단어는 문서를 구분하지 못해 변별력이 0이다. 결국 각 문서에 남는 값은 `machine`(문서1), `deep`(문서2), `like`(문서3)처럼 **특정 문서에만 쓰인 단어**들뿐이다.

단계 버튼으로 TF → IDF → TF-IDF 순서를 따라갈 수 있다. 문장을 고쳐 쓰면 사전부터 다시 계산된다.

```html preview
<div style="font-family:system-ui,sans-serif;padding:18px;color:var(--foreground)">
  <textarea id="tDocs" rows="3" style="width:100%;margin-bottom:10px;padding:10px;font-size:14px;
    font-family:ui-monospace,SFMono-Regular,Menlo,monospace;background:var(--card);
    color:var(--card-foreground);border:1px solid var(--border);
    border-radius:var(--radius);box-sizing:border-box">i love machine learning
i love deep deep learning learning
i like deep learning</textarea>

  <div id="tStep" style="display:flex;gap:6px;flex-wrap:wrap;margin-bottom:8px"></div>
  <div id="tDoc" style="display:flex;gap:6px;flex-wrap:wrap;margin-bottom:12px"></div>
  <div id="tInfo" style="padding:9px 11px;background:var(--card);border:1px solid var(--border);
    border-radius:var(--radius);font-size:12px;margin-bottom:12px;line-height:1.6"></div>
  <div id="tBars" style="display:flex;flex-direction:column;gap:4px"></div>
  <div id="tVec" style="margin-top:12px;font-size:12px;color:var(--muted-foreground);
    word-break:break-all;line-height:1.7"></div>

  <script>
    var STEPS = [['tf', '(1) TF'], ['idf', '(2) IDF'], ['prod', '(3) TF-IDF']];
    var step = 'tf', docIdx = 0;
    var ta = document.getElementById('tDocs');

    function btn(label, on, attr) {
      return '<button ' + attr + ' style="padding:5px 12px;font-size:12px;font-weight:600;' +
        'cursor:pointer;border-radius:999px;border:1px solid ' +
        (on ? 'var(--primary)' : 'var(--border)') + ';background:' +
        (on ? 'var(--primary)' : 'transparent') + ';color:' +
        (on ? 'var(--primary-foreground)' : 'var(--muted-foreground)') + '">' + label + '</button>';
    }

    function parse() {
      var lines = ta.value.split('\n').filter(function (l) { return l.trim().length > 0; });
      var toks = lines.map(function (l) {
        return l.toLowerCase().split(/[^a-z0-9가-힣]+/).filter(function (t) { return t.length > 0; });
      });
      var seen = {};
      toks.forEach(function (t) { t.forEach(function (w) { seen[w] = true; }); });
      var vocab = Object.keys(seen).sort();
      var df = {};
      vocab.forEach(function (w) {
        df[w] = toks.filter(function (t) { return t.indexOf(w) >= 0; }).length;
      });
      return { lines: lines, toks: toks, vocab: vocab, df: df, N: toks.length };
    }

    function render() {
      var D = parse();
      if (docIdx >= D.toks.length) docIdx = 0;

      document.getElementById('tStep').innerHTML =
        '<span style="font-size:12px;color:var(--muted-foreground);align-self:center;width:40px">단계</span>' +
        STEPS.map(function (s) { return btn(s[1], step === s[0], 'data-s="' + s[0] + '"'); }).join('');
      document.getElementById('tDoc').innerHTML =
        '<span style="font-size:12px;color:var(--muted-foreground);align-self:center;width:40px">문서</span>' +
        D.lines.map(function (l, i) {
          return btn('문서' + (i + 1), docIdx === i && step !== 'idf', 'data-d="' + i + '"');
        }).join('');

      var vals, subs, info;
      if (step === 'tf') {
        var t = D.toks[docIdx];
        vals = D.vocab.map(function (w) {
          return t.filter(function (x) { return x === w; }).length / t.length;
        });
        subs = D.vocab.map(function (w) {
          return t.filter(function (x) { return x === w; }).length + '/' + t.length;
        });
        info = 'TF = 빈도수 ÷ 문서 총 단어수 &nbsp;—&nbsp; 문서' + (docIdx + 1) + '은 ' + t.length + '단어';
      } else if (step === 'idf') {
        vals = D.vocab.map(function (w) { return Math.log(D.N / D.df[w]); });
        subs = D.vocab.map(function (w) { return 'df ' + D.df[w] + '/' + D.N; });
        info = 'IDF = ln(N / df), N = ' + D.N +
          ' &nbsp;—&nbsp; <b>문서와 무관하게 단어당 하나로 고정</b>된다. df = N 이면 ln(1) = 0 으로 소거.';
      } else {
        var tt = D.toks[docIdx];
        vals = D.vocab.map(function (w) {
          var tf = tt.filter(function (x) { return x === w; }).length / tt.length;
          return tf * Math.log(D.N / D.df[w]);
        });
        subs = D.vocab.map(function (w) { return 'df ' + D.df[w]; });
        info = 'TF × IDF &nbsp;—&nbsp; 방법1은 정규화 없이 여기서 끝이다.';
      }

      var max = Math.max.apply(null, vals.concat([0.0001]));
      document.getElementById('tInfo').innerHTML = info;
      document.getElementById('tBars').innerHTML = D.vocab.map(function (w, k) {
        var v = vals[k], zero = v === 0;
        var col = zero ? 'var(--border)' : (v === max ? 'var(--chart-5)' : 'var(--chart-1)');
        return '<div style="display:flex;align-items:center;gap:10px;opacity:' + (zero ? '0.45' : '1') + '">' +
          '<div style="width:70px;text-align:right;font-size:13px;white-space:nowrap">' + w + '</div>' +
          '<div style="width:52px;font-size:10px;color:var(--muted-foreground);white-space:nowrap">' + subs[k] + '</div>' +
          '<div style="flex:1;height:16px;background:var(--border);border-radius:4px;overflow:hidden;min-width:40px">' +
          '<div style="width:' + (v / max * 100) + '%;height:100%;background:' + col + '"></div></div>' +
          '<div style="width:58px;font-size:12px;font-weight:700;font-variant-numeric:tabular-nums;' +
          'text-align:right;color:' + (zero ? 'var(--muted-foreground)' : col) + '">' + v.toFixed(4) + '</div></div>';
      }).join('');

      document.getElementById('tVec').innerHTML =
        '<b>' + (step === 'idf' ? 'IDF (전체 공통)' : '문서' + (docIdx + 1)) + '</b><br>[' +
        vals.map(function (v) { return v.toFixed(4); }).join(', ') + ']';

      Array.prototype.forEach.call(document.querySelectorAll('#tStep button'), function (b) {
        b.onclick = function () { step = b.getAttribute('data-s'); render(); };
      });
      Array.prototype.forEach.call(document.querySelectorAll('#tDoc button'), function (b) {
        b.onclick = function () {
          docIdx = +b.getAttribute('data-d');
          if (step === 'idf') step = 'tf';
          render();
        };
      });
    }

    ta.addEventListener('input', render);
    render();
  </script>
</div>
```

### 방법2 — 싸이킷런 방식 (p.13\~17)

$$
\text{TF} = \text{빈도수(raw)}
\qquad
\text{IDF} = \ln\!\left(\frac{N+1}{df+1}\right) + 1
$$

- IDF: `학교` 1.2231 · `친구` 1.5108 · 나머지 1.9163 (`+1` 스무딩으로 0나눗셈·가중치 소멸 방지)
- TF×IDF 후 **각 문서를 자기 norm으로 나눈다**(L2). 문서1은 $\|\vec{d_1}\| \approx 2.9737$
- 문서1 → `나` 0.6445, `학교` 0.4114, `간다` 0.6445

### 두 방법의 차이

- 단어사전은 동일, **TF 정의·IDF 수식·정규화 유무**에서 갈라진다
- 문서 길이 보정: 방법1은 TF 단계, 방법2는 마지막 L2 정규화
- 벡터 크기: 방법1은 문서마다 제각각, 방법2는 모두 norm = 1
- ⚠️ TF-IDF 수식은 **하나로 정해져 있지 않다** (p.7). 어떤 정의를 썼는지 밝혀야 한다

<details>
<summary>방법1 문서2~4 벡터 (p.12 과제)</summary>

사전 순서 `['간다','갔다','공부한다','나','다른','만나서','운동','좋아하는','집','친구','학교','함께','했다']`

```text
문서1  [0.46, 0,    0,    0.46, 0,    0,    0,    0,    0,    0,    0.10, 0,    0   ]
문서2  [0,    0,    0.69, 0,    0,    0,    0,    0,    0,    0,    0.14, 0,    0   ]
문서3  [0,    0.20, 0,    0,    0.20, 0.20, 0,    0,    0.20, 0.20, 0.04, 0,    0   ]
문서4  [0,    0,    0,    0,    0,    0,    0.46, 0.23, 0,    0.12, 0,    0.23, 0.23]
```

문서3의 `친구`는 2번 나왔는데 1번만 나온 `갔다`·`집`과 같은 0.20이다. $df = 2$ 일 때 $\ln(4/2)$ 가 $\ln(4)/2$ 이므로 TF 2배와 IDF 절반이 상쇄된다.

검증: 동일 로직으로 방법2를 계산해 강의 p.17 최종값(0.6445 / 0.8429 / 0.6006 / 0.7244 …)과 소수점 4자리까지 일치함을 확인했다.

</details>

### 싸이킷런 (p.18)

```python
from sklearn.feature_extraction.text import TfidfVectorizer
import pandas as pd

docs = ["나 학교 간다", "학교 공부한다",
        "친구 학교 만나서 다른 친구 집 갔다",
        "운동 좋아하는 친구 함께 운동 했다"]

vectorizer = TfidfVectorizer(token_pattern=r"(?u)\b\w+\b")   # 1글자 단어 허용
tfidf = vectorizer.fit_transform(docs)
print(pd.DataFrame(tfidf.toarray(), columns=vectorizer.get_feature_names_out()))
```

> [!WARNING]
> `token_pattern`을 생략하면 기본값이 **2글자 이상**만 토큰으로 잡아 `나`, `i` 같은 1글자 단어가 버려진다. 단어사전 크기가 달라져 정답이 틀어진다.

## 5. 확인 문제

### OX — 제목을 눌러 정답 확인

<details>
<summary>1. TF는 같은 단어라도 문서마다 값이 다르다.</summary>

**⭕** — TF는 (단어, 문서) 쌍의 함수다. `학교`의 TF는 문서2에서 0.50, 문서3에서 0.14로 다르다.

</details>

<details>
<summary>2. IDF도 같은 단어라도 문서마다 값이 다르다.</summary>

**❌** — IDF는 코퍼스 전체로 계산하는 (단어)만의 함수라 **단어 1개당 값이 1개**다. 문서가 바뀜어도 그대로.

</details>

<details>
<summary>3. 여러 문서에 두루 등장하는 단어일수록 IDF가 높아진다.</summary>

**❌** — 반대다. $df$가 분모에 있어 흔할수록 IDF가 낮아진다. 흔한 단어를 깎는 게 IDF의 역할.

</details>

<details>
<summary>4. BoW 벡터만 보면 원래 문장의 단어 순서를 복원할 수 있다.</summary>

**❌** — 순서·문맥·의미가 모두 사라진다. 그래서 이름이 단어"가방" 모델이다.

</details>

<details>
<summary>5. binary BoW와 count BoW는 항상 서로 다른 벡터를 만든다.</summary>

**❌** — 중복 단어가 없으면 완전히 같다. 과제의 문서1·문서3이 그 경우.

</details>

<details>
<summary>6. 단어사전의 순서가 바뀌면 같은 문서라도 벡터가 달라진다.</summary>

**⭕** — 사전의 $i$번째 단어가 벡터의 $i$번째 차원이다. 그래서 정렬해 고정한다.

</details>

<details>
<summary>7. 방법1에서는 문서 길이가 TF 값에 영향을 준다.</summary>

**⭕** — 빈도수를 문서 총 단어수로 나누므로 짧은 문서일수록 단어 하나의 TF가 커진다.

</details>

<details>
<summary>8. 방법2(싸이킷런)도 TF 단계에서 문서 길이로 나눈다.</summary>

**❌** — 방법2의 TF는 raw count 그대로. 문서 길이는 마지막 L2 정규화에서 반영된다.

</details>

<details>
<summary>9. TF-IDF를 구하는 수식은 하나로 정해져 있다.</summary>

**❌** — p.7. 로그 밑, 스무딩, 정규화 방식이 구현마다 다르다.

</details>

<details>
<summary>10. 단어 수가 많아질수록 BoW 행렬은 희소(sparse)해진다.</summary>

**⭕** — 한 문서가 쓰는 단어는 사전 전체의 극일부라 차원이 커질수록 0의 비율이 늘어난다.

</details>

### 생각해볼 포인트 (p.12)

<details>
<summary>① 여러 문서에서 등장하면 IDF가 낮은가요?</summary>

**낮아진다.** `학교`는 4문서 중 3개에 등장해 IDF 0.29, 1개 문서에만 있는 `간다`는 1.39 — 약 4.8배 차이로 흔한 단어가 억제된다.

</details>

<details>
<summary>② 긴 문서와 짧은 문서가 TF 차이가 나나요?</summary>

**방법1은 난다.** `학교`가 문서2(2단어)에선 $1/2 = 0.50$, 문서3(7단어)에선 $1/7 \approx 0.14$.

**방법2는 TF 단계에선 안 난다**(둘 다 1). 대신 L2 정규화가 그 역할을 해서 최종값은 0.5380 vs 0.2431로 벌어진다.

</details>

<details>
<summary>③ 같은 단어가 문서별로 TF, IDF 값이 다른가요?</summary>

**TF는 다르고, IDF는 같다.** TF는 (단어, 문서)의 함수, IDF는 (단어)만의 함수다. 따라서 최종 TF-IDF가 문서마다 달라지는 원인은 전적으로 TF 쪽이다.

</details>

## 6. 정리

- **단어사전** — 정렬해 고정. 단어 종류 수 = 벡터 차원
- **binary / count** — 있다없다 vs 몇 번. 중복 단어가 없으면 둘은 같다
- **TF** — 문서마다 다름 / **IDF** — 단어당 1개로 고정
- **TF-IDF** — 이 문서에만 자주 나오는 단어에 높은 점수
- BoW·TF-IDF 모두 **순서를 잃는다** → Word2Vec·BERT 임베딩으로 확장

> [!NOTE]
> 관련 — [01. 빅데이터 분석의 정의와 특성 - 3V 모델, 통계적 멱법칙(Power Law)과 TF-IDF 텍스트 마이닝](../../big-data-analysis/notes/01.%20%EB%B9%85%EB%8D%B0%EC%9D%B4%ED%84%B0%20%EB%B6%84%EC%84%9D%EC%9D%98%20%EC%A0%95%EC%9D%98%EC%99%80%20%ED%8A%B9%EC%84%B1%20-%203V%20%EB%AA%A8%EB%8D%B8%2C%20%ED%86%B5%EA%B3%84%EC%A0%81%20%EB%A9%B1%EB%B2%95%EC%B9%99%28Power%20Law%29%EA%B3%BC%20TF-IDF%20%ED%85%8D%EC%8A%A4%ED%8A%B8%20%EB%A7%88%EC%9D%B4%EB%8B%9D.md)
